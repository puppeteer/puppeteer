name: Task
description: Create a task to track work
title: 'Work tracking for mainainers only!'
type: 'Task'
body:
  - type: markdown
    attributes:
      value: |
        ### This issue type should be used only by mainainers!
        Task are to track small non user facing issue or impromvents. 
        The issue will be closed if it does not follow those rule.
    validations:
      required: true
