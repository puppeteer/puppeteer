function subarraySum(arr, n, s)
    {
        //your code here
            
     for(index = 0,index < arr.lenth,index++){
         let k = S-arr[index]
         let j = arr.indexof(k)
         console.log(j)
     }
    
    }
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }