{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Puppeteer Target",
  "description": "Puppeteer target for Angular",
  "type": "object",
  "properties": {
    "build": {
      "type": "string",
      "description": "Command to transpile the e2e tests"
    },
    "serve": {
      "type": "string",
      "description": "Command to start the server"
    },
    "test": {
      "type": "string",
      "description": "Command to run the e2e tests with test runner"
    }
  }
}
