{
  "title": "Puppeteer",
  "description": "Options for Puppeteer Angular Schematics",
  "type": "object",
  "properties": {
    "commands": {
      "type": "array",
      "items": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1
      },
      "description": "Custom commands to run instead of the selected test runner. Commands are prefixed with `./node_modules/.bin` (Exception: 'node')."
    },
    "devServerTarget": {
      "type": "string",
      "description": "Angular target that spawns the server."
    },
    "port": {
      "type": ["number", "null"],
      "description": "Port to run the test server on."
    },
    "baseUrl": {
      "type": ["string", "null"],
      "description": "Base Url to run test ageist. If provided no test server will be spawned."
    }
  },
  "additionalProperties": true
}
