{
  "name": "@puppeteer-test/smoke",
  "version": "latest",
  "private": true,
  "scripts": {
    "build": "exit 0",
    "test": "wireit"
  },
  "wireit": {
    "test": {
      "command": "node ./run-smoke.mjs",
      "clean": "if-file-deleted",
      "files": [
        "src/**"
      ],
      "output": [
        "build/**",
        "tsconfig.tsbuildinfo"
      ]
    }
  }
}
